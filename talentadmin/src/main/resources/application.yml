spring:
  application:
    name: talentadmin
  security:
    user:
      name: "admin"
      password: "123456"
  mail:
    host: pop3.mxhichina.com
    username: Javaweb@vbooster.cn
    password: Vbooster@123
  boot:
    admin:
      notify:
        mail:
          to: Javaweb@vbooster.cn
server:
  port: 8769
eureka:
  client:
    service-url:
      defaultZone:  ${EUREKA_SERVICE_URL:http://localhost:8761}/eureka/
  instance:
    health-check-url-path: /actuator/health
    metadata-map:
      user.name: ${spring.security.user.name}
      user.password: ${spring.security.user.password}
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS